# The false-positive check for Rollouts analysis.
#
# Nothing is changed but the nonce, so every rollout here is a GOOD one. Run it with
# EXPERIMENT_REPEAT=10 and count how many the deploys fail anyway:
#
#   EXPERIMENT_REPEAT=10 flow run experiment good-revision
